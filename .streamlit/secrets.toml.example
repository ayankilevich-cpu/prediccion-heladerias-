# Ejemplo para Streamlit Cloud: Settings → Secrets
# Copiar las claves que uses (sin # al inicio de la línea activa).

# URL HTTPS que recibe POST con cuerpo JSON (Zapier, Make, Google Apps Script, etc.)
# USO_LOG_WEBHOOK = "https://tu-endpoint.com/registro"

# Opcional: se envía como Authorization: Bearer <valor>
# USO_LOG_API_KEY = "tu_token_secreto"
